# Shared passphrase people type on the landing screen.
SITE_PASSWORD=

# Signs the session cookie. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# Changing this signs everyone out.
SESSION_SECRET=
